# model-info 命令功能说明

> **版本**: v1.0.0
> **日期**: 2026-01-31 23:01:28

---

## 📖 功能简介

`model-info.md` 是 Claude Code 的自定义命令，用于查看当前大模型配置信息。

---

## ✨ 核心功能

### 1. 查看当前模型
显示 `model-config.json` 中配置的模型名称。

### 2. 查看 API 端点
显示本地代理服务器的地址。

### 3. 查看认证方式
显示 API 认证模式（如 PROXY_MANAGED）。

### 4. 确认 Statusline 同步状态
验证 statusline 是否正确配置为读取 `model-config.json`。

---

## 📋 使用方法

在 Claude Code 对话中输入：
```
/model-info
```

---

## 📤 输出示例

```
# 🤖 Claude Code 模型配置信息

## ✅ 当前模型
**glm-4.7**

## 🔗 API 端点
`http://127.0.0.1:5000`

## 🔑 认证方式
**PROXY_MANAGED**（由本地代理服务器管理）

## 🔄 Statusline 同步状态
✅ Statusline 已配置为从 model-config.json 实时读取模型
```

---

## 📁 文件位置

```
~/.claude/commands/model-info.md
```

---

## 🔧 命令结构

```yaml
---
description: 查看当前大模型配置信息并同步到 statusline
argument-hint: ""
allowed-tools: Read(~/.claude/model-config.json, ...)
---

## 用法
`/model-info`

## 目标
查看当前 Claude Code 的大模型配置...

## 执行步骤
**步骤 1**：读取 `~/.claude/model-config.json`
**步骤 2**：读取 `~/.claude/settings.json`
...
```

---

## 🛠️ 技术细节

### YAML Frontmatter
命令的元数据配置，包括：
- `description`: 命令描述
- `argument-hint`: 参数提示
- `allowed-tools`: 允许使用的工具列表

### Markdown 内容
命令的执行指南，包括：
- 用法说明
- 执行步骤
- 输出格式

---

## 🔄 相关文件

| 文件 | 作用 |
|------|------|
| `~/.claude/model-config.json` | 存储当前模型配置 |
| `~/.claude/settings.json` | 存储 API 端点和认证配置 |
| `~/.claude/config.json` | Claude Code 主配置 |
| `~/.claude/statusline-command.sh` | Statusline 脚本 |

---

## 💡 使用场景

### 场景 1: 确认当前使用的模型
```
/model-info
→ 显示当前模型：glm-4.7
```

### 场景 2: 检查 API 配置
```
/model-info
→ 显示 API 端点：http://127.0.0.1:5000
→ 显示认证方式：PROXY_MANAGED
```

### 场景 3: 验证 Statusline 同步
```
/model-info
→ 确认 Statusline 是否正确读取 model-config.json
```

---

## 🐛 常见问题

### Q: 命令不生效？
**A**: 确认文件已放置在 `~/.claude/commands/` 目录下，并重启 Claude Code。

### Q: 无法读取配置文件？
**A**: 检查 `allowed-tools` 中是否包含正确的文件路径。

### Q: Statusline 显示不一致？
**A**: 检查 `statusline-command.sh` 是否正确配置了读取 `model-config.json`。
